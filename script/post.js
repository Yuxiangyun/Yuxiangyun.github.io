(function($){
  // 移除移动端自动隐藏 header 的逻辑，确保 header 在所有设备上都能显示
  // var $header = $('#header');
  // if (window.innerWidth < 480 && !$header.hasClass('slide-hide')) {
  //   $header.addClass('slide-hide');
  // }
})(jQuery);
